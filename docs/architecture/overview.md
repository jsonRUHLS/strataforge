# Architecture Overview

StrataForge is organized as a pnpm workspace with one primary web application and shared packages.

## Main parts

- Web application: the main interface and runtime entry point
- Content package: structured content records and metadata
- Schema package: shared types and validation rules
- Integrations package: stack matching and helper logic
- UI package: reusable interface components
